# Config Migrations

This directory stores config migration scripts between schema versions.

Naming convention:
- `v{from}_to_v{to}.py`

Each migration should be deterministic and emit a machine-readable change summary.

